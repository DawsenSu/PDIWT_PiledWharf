<UserControl
    x:Class="PDIWT.Resources.CustomUserControl.LabelTextBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PDIWT.Resources.CustomUserControl"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="20"
    d:DesignWidth="40"
    mc:Ignorable="d">
    <Grid x:Name="root">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="90" />
            <ColumnDefinition Width="60" />
        </Grid.ColumnDefinitions>
        <Label
            Grid.Column="0"
            Content="{Binding Path=DisplayName, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type local:LabelTextBox}}}"
            Foreground="{Binding Path=Foreground, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type local:LabelTextBox}}}" />
        <TextBox
            Grid.Column="1"
            Margin="1"
            Text="{Binding Path=Parameter, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type local:LabelTextBox}}}" />
        <Label Grid.Column="2" Content="{Binding Path=Unit, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type local:LabelTextBox}}}" />
    </Grid>
</UserControl>
